// A real paragraph per port. The generated pages previously carried about
// thirteen words of port-specific content inside four hundred words of shared
// lsof/netstat boilerplate, which is both useless to a reader and
// indistinguishable from its siblings to a search engine. Content that varies
// with the item has to live in the data, not the template.
export const DETAIL = {
  20: 'Classic FTP uses two connections: commands on 21 and file contents on 20. In <em>active</em> mode the server opens the data connection back to the client from port 20, which is why active FTP breaks behind almost every NAT and firewall built since the 1990s — the inbound connection has nowhere to land. Passive mode was invented to fix this: the client opens both connections, and the server picks a high port for data instead. If FTP transfers hang after a successful login while the directory listing works, active versus passive is the first thing to check.',
  21: 'Port 21 carries the control conversation only: <code>USER</code>, <code>PASS</code>, <code>LIST</code>, <code>RETR</code>, all in plain text and all readable by anything on the path. The greeting banner usually announces the server software and version, which is why scanners find and fingerprint FTP servers within hours of one appearing. FTPS adds TLS to this same port; SFTP is unrelated despite the name and runs over SSH on 22.',
  22: 'SSH multiplexes far more than a shell: SFTP, SCP, port forwarding, and Git all ride the same connection. That makes 22 the most valuable port on most machines and, correspondingly, the most attacked — an internet-facing host sees automated password attempts within minutes. The effective defences are key-only authentication and rate limiting; moving to a high port cuts log noise but stops nothing that scans the full range. If a connection hangs after the banner rather than failing outright, the usual cause is a MTU or keepalive problem rather than authentication.',
  23: 'Telnet sends everything as plain text, including the password, and has no host verification at all — there is no way to know you are talking to the machine you meant. It was superseded by SSH in the 1990s and survives mainly in network equipment and industrial controllers that were never updated. The one legitimate modern use is as a crude connectivity test: <code>telnet host 25</code> to see whether a port answers, which is why the client is still installed even where the service is not.',
  25: 'Port 25 is server-to-server mail relay, not mail submission. Nearly every residential ISP and cloud provider blocks outbound 25 by default because it is the port spam is sent from — if your application cannot send mail from a cloud VM, this is almost always why. Use 587 with authentication for sending. An open relay on 25 will be found and abused within days, and the resulting blacklisting takes far longer to undo than it took to happen.',
  53: 'DNS uses UDP for ordinary queries and TCP for anything over 512 bytes, including zone transfers and most DNSSEC responses. Firewalls that allow only UDP 53 produce a characteristic failure: most lookups work, and specific ones — usually the DNSSEC-signed or unusually large records — time out. An open recursive resolver is a serious liability because a small query can be made to produce a large answer, which makes it an amplifier for denial-of-service attacks against someone else.',
  67: 'The server half of DHCP. A client with no address broadcasts to 67, and the server answers to 68. Because it is broadcast-based, two DHCP servers on one segment will both answer and clients get whichever reply arrives first — the classic cause of intermittent, unexplainable network failures in small offices, usually created by someone plugging a home router into a corporate switch.',
  68: 'The client half of DHCP. Nothing listens here on a server; it is where a workstation waits for its address offer. If a machine ends up with a 169.254.x.x address, the DHCP exchange failed and the operating system fell back to link-local addressing. That is a symptom of the network, not the machine — cable, VLAN, or an exhausted DHCP pool.',
  69: 'Trivial FTP has no authentication whatsoever by design and runs over UDP. It exists because it is small enough to fit in the boot ROM of a device that has no operating system yet, which is why it is still used for PXE network boot and for pushing firmware to switches and phones. It should exist only on an isolated provisioning network and should never be reachable from anywhere else.',
  80: 'Plain HTTP. The only thing that should be served here now is a permanent redirect to HTTPS, and even that is a transitional measure: with HSTS the browser stops trying port 80 at all after its first visit. Port 80 remains useful for the ACME HTTP-01 challenge that Let\'s Encrypt uses to issue certificates, which is the usual reason it cannot simply be closed.',
  110: 'POP3 downloads messages and, by default, deletes them from the server. That model predates having more than one device — read your mail on a laptop with POP3 and it is not on your phone. IMAP replaced it for that reason. Plain POP3 on 110 is unencrypted; 995 is the TLS version. If a mail client is configured for POP3 and messages keep disappearing from webmail, this is the explanation.',
  123: 'NTP keeps clocks synchronised, which matters more than it sounds: TLS certificate validation, Kerberos authentication, TOTP two-factor codes and log correlation all fail in confusing ways when a clock is wrong by minutes. The protocol is UDP and the traffic is tiny, so the usual failure is a firewall silently dropping it and a clock that drifts for weeks before anyone connects the symptoms.',
  143: 'IMAP keeps messages on the server and synchronises state — read, flagged, filed — so every device sees the same mailbox. This is the fundamental difference from POP3 on 110, and the reason IMAP won. Port 143 is unencrypted or STARTTLS-upgraded; 993 is implicit TLS from the first byte. A mail client that works on one and not the other is almost always configured for the wrong one of those two.',
  161: 'SNMP polls device statistics — interface counters, temperatures, uptime. Versions 1 and 2c authenticate with a community string sent in clear text, and "public" is still a common default, which means an exposed SNMP agent hands over a full inventory of the device to anyone who asks. Version 3 adds real authentication and encryption. Like DNS and NTP, SNMP is also an amplification vector because a small query can return a large table.',
  389: 'LDAP is the directory lookup protocol behind Active Directory and most enterprise single sign-on. A bind operation sends a username and password, and on port 389 without StartTLS it sends them in clear text — which is why an LDAP misconfiguration leaks credentials for the entire organisation rather than one account. 636 is the implicit-TLS equivalent. Modern Windows domain controllers reject unsigned simple binds by default for exactly this reason.',
  443: 'HTTPS, and effectively the whole modern web. Worth knowing that HTTP/3 runs over QUIC on <strong>UDP</strong> 443, not TCP: a firewall that permits only TCP 443 does not break anything visibly, because clients silently fall back to HTTP/2, but the performance benefit is lost and the cause is invisible in logs. A TLS handshake failing immediately after the connection opens is usually a certificate or SNI problem rather than a network one.',
  445: 'SMB file and printer sharing. This port should never be reachable from the internet under any circumstances — it is how WannaCry and NotPetya spread, and exposed SMB remains one of the most reliably exploited services in existence. Many ISPs block it outbound for their customers\' own protection. Inside a network it is unavoidable and fine; crossing a perimeter it is an incident.',
  465: 'Implicit-TLS mail submission. Its history is unusual: originally assigned for SMTPS, then deprecated in favour of STARTTLS on 587, then formally reinstated in 2018 because implicit TLS turned out to be the safer design — there is no plaintext phase for an attacker to strip. Both 465 and 587 are correct today; 587 has wider support in older clients.',
  514: 'Remote syslog over UDP, which means no authentication, no encryption and no delivery guarantee — messages can be forged, read, or silently lost under load. This is acceptable for informational logging on a trusted network and unacceptable for anything security-relevant, since the logs you would need after an incident are exactly the ones an attacker can suppress. TLS syslog on 6514 solves both problems.',
  587: 'The correct port for an application to send mail. Unlike 25 it expects authentication, is rarely blocked by ISPs or cloud providers, and is intended for a mail client or application handing a message to its own server rather than server-to-server relay. If outbound mail from an application fails and you are using port 25, moving to 587 with credentials fixes it more often than any other single change.',
  636: 'LDAP wrapped in TLS from the first byte. Preferred over StartTLS on 389 in most deployments because there is no unencrypted phase at all — nothing to strip, nothing to downgrade. The usual failure here is certificate validation: directory servers are frequently issued internal certificates, and a client that does not trust the internal CA fails with an error that mentions neither the CA nor the certificate.',
  993: 'IMAP with TLS from the first byte, and the port essentially every mail client uses today. The difference from 143 is not the protocol but when encryption starts: 993 negotiates TLS before any IMAP command, while 143 begins in the clear and upgrades with STARTTLS, which can be stripped by an attacker on the path. If you have a choice, this is the one to configure.',
  995: 'POP3 over TLS. The encryption is the only thing this shares with 993 — the protocol underneath is fundamentally different. POP3 downloads and typically deletes, keeping no server-side state, so a second device sees an empty mailbox. IMAP on 993 synchronises instead. Choosing 995 over 993 is a decision about how mail is stored, not about security, and it is almost always the wrong one for anyone with more than one device.',
  1080: 'The SOCKS proxy port, and what <code>ssh -D 1080</code> opens for a dynamic tunnel. SOCKS operates below the application layer, so it proxies anything TCP rather than just HTTP. An open SOCKS proxy reachable from the internet will be found and used to launder traffic within days, which is why the SSH form should always be bound to localhost rather than to all interfaces.',
  1433: 'Microsoft SQL Server. Named instances complicate this: the SQL Browser service on UDP 1434 tells clients which port a named instance is actually using, so blocking 1434 breaks named-instance connections while the default instance on 1433 keeps working. Like every database port, this should be reachable only from application servers, never from the internet — exposed SQL Server instances are scanned for continuously.',
  1521: 'The Oracle listener. The listener is a directory rather than the database itself: it accepts a connection, identifies the requested service, and hands the session to a database process which may continue on a different port. That indirection is why Oracle connectivity through a firewall is more involved than for other databases, and why "the listener is up" and "the database is reachable" are separate questions.',
  1883: 'MQTT, the publish-subscribe protocol behind most IoT deployments. Plain 1883 has no encryption, and MQTT brokers are frequently deployed with anonymous access enabled because that is the quickest path to a working prototype. An exposed broker lets anyone subscribe to every topic — which in practice has meant reading live sensor data and, worse, publishing commands to devices. 8883 is the TLS port.',
  2049: 'NFS, network file sharing on Unix. Traditional NFS trusts the client to report which user is making a request, so a machine you do not control that can reach this port can claim to be any user on the exported filesystem. Modern versions can use Kerberos, but the default in most deployments is still the trusting model, which is why NFS exports belong on a private network with explicit host restrictions.',
  2375: 'The Docker daemon API, unencrypted and unauthenticated. Anyone who can reach this port can start a container that mounts the host filesystem, which is root access to the machine with no exploit required. It is not a vulnerability so much as a remote root shell offered deliberately, and exposed Docker APIs are one of the most commonly abused misconfigurations on the internet. If remote Docker access is genuinely needed, use 2376 with TLS, or SSH.',
  2376: 'The Docker daemon API with TLS and client-certificate authentication. This is the supported way to reach a daemon remotely, and the certificate requirement is the entire point — without it the API grants root, as port 2375 demonstrates. Even so, an SSH tunnel is usually simpler to operate and easier to audit than distributing and rotating client certificates.',
  3000: 'No standard assignment; it became a convention because it is easy to type and above the 1024 privileged boundary. Node and Express default to it, as do Rails, Grafana and a long list of others — which is why "port 3000 is already in use" is one of the most common errors in development. It is a development port: in production a reverse proxy holds 443 and forwards here.',
  3306: 'MySQL and MariaDB. The most important thing about this port is that it should not be reachable from the internet, and the second is that <code>localhost</code> and <code>127.0.0.1</code> are not the same thing to a MySQL client — the first uses a Unix socket and the second uses TCP on this port, and they can have different permissions and even connect to different servers. "Access denied for localhost" that works with 127.0.0.1 is that distinction.',
  3389: 'Remote Desktop. Exposed RDP is the single most common initial access point for ransomware, by a wide margin: credential-stuffing against 3389 is fully automated and continuous. It belongs behind a VPN or a gateway with multi-factor authentication. Network Level Authentication helps but does not make direct exposure acceptable — the attacks that matter are against valid credentials, not against the protocol.',
  4200: 'The Angular CLI development server. Like 3000 and 5173 it is a convention rather than an assignment, chosen to be memorable and unprivileged. It serves an unminified build with source maps and a live-reload websocket, none of which should ever face the internet — the development server is not hardened and is not intended to be.',
  5000: 'Flask\'s default, and also used by .NET and several others — which is why it collides so often. On macOS it collides with something less obvious: AirPlay Receiver binds 5000 by default on recent versions, so a Flask app that has always worked suddenly fails to bind after an OS upgrade. Disabling AirPlay Receiver in System Settings or moving the app resolves it.',
  5173: 'Vite\'s development server. The number is deliberate: 5173 spells "VITE" on a phone keypad. Like other development servers it serves unminified code with a hot-module-reload websocket and should stay on localhost. Vite will pick the next free port automatically if this one is taken, which is convenient and occasionally confusing when a stale process is still holding it.',
  5432: 'PostgreSQL. Worth knowing that <code>pg_hba.conf</code> governs who may connect and how, independently of the port and of the database user\'s own permissions — a connection can be refused there before any password is checked, which is why "no pg_hba.conf entry for host" is a configuration error rather than a credentials error. As with every database, this port should be private.',
  5672: 'AMQP, the protocol behind RabbitMQ. This is the message traffic itself; the management web interface is a separate service on 15672 and is the one that ships with a default guest/guest account. That account is restricted to localhost in current versions, but older deployments and hastily written container images frequently expose it, which grants full control of the broker.',
  6379: 'Redis. Until version 6 there was no user model at all and, in default configurations, no password either — anyone who could reach the port had complete control. Redis can write files to arbitrary paths, so that control has repeatedly been turned into remote code execution by writing an SSH key or a cron entry. Modern versions bind to localhost and enable protected mode by default; the danger is old configurations and containers that override it.',
  8000: 'A conventional alternative to 80 for anything unprivileged, and the default for Python\'s <code>http.server</code> and Django\'s development server. Because it is so commonly used for quick local sharing, it is also commonly forgotten about and left running — a directory listing on 8000 exposed to a network is a small but real information leak.',
  8080: 'The most widely used HTTP alternative port, historically the standard for Tomcat and for HTTP proxies. Its ubiquity is the problem: 8080 is scanned as thoroughly as 80, so an "internal" service on 8080 with a public IP is found immediately. It is also the most frequent collision in development, since so many tools default to it.',
  8443: 'The conventional HTTPS equivalent of 8080, used where a process cannot bind 443 without root. Management interfaces for firewalls, appliances and application servers commonly live here — which makes it a high-value target, since a service on 8443 is far more likely to be an administrative console than an ordinary site.',
  8883: 'MQTT over TLS. The same protocol as 1883 with transport encryption, and the one that should be used for anything crossing a network you do not fully control. Encryption alone is not enough: brokers still need authentication and topic-level authorisation, because TLS protects the channel while leaving an anonymous client free to subscribe to everything on it.',
  9000: 'Heavily overloaded. PHP-FPM listens here for FastCGI, and so do SonarQube, Portainer and MinIO among others. The PHP-FPM case is the one that matters for security: the FastCGI protocol assumes it is only reachable from the web server, and an exposed PHP-FPM socket can be made to execute arbitrary files. It should be bound to localhost or, better, to a Unix socket.',
  9090: 'Prometheus\'s web interface and API, and also Cockpit on some Linux distributions. Prometheus has no authentication of its own by design — the expectation is that it sits behind a reverse proxy that provides it. An exposed Prometheus hands over a complete map of the infrastructure it monitors: hostnames, service names, versions and traffic patterns.',
  9200: 'The Elasticsearch HTTP API. Unauthenticated clusters exposed on this port have produced some of the largest data leaks on record, repeatedly, because the API allows reading and deleting every index with an ordinary HTTP request and no credentials. Security is enabled by default in current versions; the exposures come from old clusters and from tutorials that disable it to get started quickly.',
  11211: 'Memcached, which has no authentication in its standard configuration. Beyond the obvious risk of an exposed cache, the UDP interface was used for record-breaking amplification attacks in 2018 — a small forged request produced a response thousands of times larger, aimed at a victim. UDP is disabled by default now as a direct result. Bind it to localhost.',
  27017: 'MongoDB. Versions before 3.6 bound to all interfaces by default with no authentication, which produced a long series of mass-ransom incidents where databases were wiped and a bitcoin address left in their place. The default is now localhost, but the pattern still appears in containers and cloud images that override it for convenience.',
};
